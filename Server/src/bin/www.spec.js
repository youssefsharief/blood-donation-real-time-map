require('jasmine')
// const www = require('../../src/bin/www')


describe("onListening", function () {

    it("users should get notified and get speaker's name and the title of presentation", function (done) {
        // const server = {
        //     address: ()=> "rendom address"
        // }
        // const debug = (str)=>{} 
        // www.onListening()
        done()
    });

})